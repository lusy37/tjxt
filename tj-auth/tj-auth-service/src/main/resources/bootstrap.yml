server:
  port: 8081  #端口
  tomcat:
    uri-encoding: UTF-8   #服务编码
spring:
  profiles:
    active: dev
  application:
    name: auth-service
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs: # 共享配置
          - data-id: shared-spring.yaml # 共享spring配置
            refresh: false
          - data-id: shared-redis.yaml # 共享redis配置
            refresh: false
          - data-id: shared-mybatis.yaml # 共享mybatis配置
            refresh: false
          - data-id: shared-logs.yaml # 共享日志配置
            refresh: false
encrypt:
  key-store:
    alias: ${ENCRYPT_KEY_STORE_ALIAS}  # 环境变量：ENCRYPT_KEY_STORE_ALIAS
    location: ${ENCRYPT_KEY_STORE_LOCATION}  # 环境变量：ENCRYPT_KEY_STORE_LOCATION
    password: ${ENCRYPT_KEY_STORE_PASSWORD}  # 环境变量：ENCRYPT_KEY_STORE_PASSWORD
    secret: ${ENCRYPT_KEY_STORE_SECRET}  # 环境变量：ENCRYPT_KEY_STORE_SECRET
tj:
  swagger:
    enable: true
    enableResponseWrap: true
    package-path: com.tianji.auth.controller
    title: 天机学堂 - 权限微服务接口文档
    description: 该服务用于管理权限相关信息
    contact-name: 传智教育·研究院
    contact-url: http://www.itcast.cn/
    contact-email: zhanghuyi@itcast.cn
    version: v1.0
  auth:
    resource:
      enable: true
      includeLoginPaths:
        - /menus/me
        - /accounts/logout
  jdbc:
    database: tj_auth